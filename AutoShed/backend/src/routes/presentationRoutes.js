// import express from "express";
// import { bookPresentation, reschedulePresentation,getPresentations } from "../controllers/presentationController.js";

// const router = express.Router();

// router.post("/", bookPresentation); // Book a slot
// router.put("/:id/reschedule", reschedulePresentation); // Reschedule a slot
// router.get("/", getPresentations); // Get all scheduled presentations



// export default router;
